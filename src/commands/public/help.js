export default {
    active: true,
    // type: 'public', // ?

    trigger: 'help',
    handler: (bot) => {
        console.log('!!! computer says no!');
        // bot.say('Computer says no!');
    }
};
